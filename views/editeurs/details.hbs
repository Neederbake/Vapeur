<div class="container">
	<div class="page-header">
		<h1>{{editeurs.nom}}</h1>
		<div class="actions">
			<a href="/editeurs/{{editeurs.id}}/edit" class="btn btn-secondary">✏️ Modifier</a>
			<form action="/editeurs/{{editeurs.id}}/delete" method="POST" style="display:inline" onsubmit="return confirm('Supprimer cet éditeur ?');">
				<button type="submit" class="btn btn-danger">🗑️ Supprimer</button>
			</form>
		</div>
	</div>

	{{#if editeurs.jeux_publies}}
		<h2>Jeux publiés</h2>
		<ul class="simple-list">
			{{#each editeurs.jeux_publies}}
				<li>{{this.titre}}</li>
			{{/each}}
		</ul>
	{{else}}
		<p>Aucun jeu publié pour cet éditeur.</p>
	{{/if}}
</div>
