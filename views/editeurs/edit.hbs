<div class="container">
    <h1>Modifier l'éditeur</h1>
    
    <form action="/editors/{{editor.id}}/edit" method="POST" class="form-card">
        <div class="form-group">
            <label for="nom">Nom de l'éditeur *</label>
            <input 
                type="text" 
                id="nom" 
                name="nom" 
                class="form-input" 
                value="{{editor.nom}}"
                placeholder="Ex: Ubisoft, Electronic Arts, Activision..."
                required
            >
        </div>

        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
            <a href="/editors/{{editor.id}}" class="btn btn-secondary">Annuler</a>
        </div>
    </form>
</div>

<style>
.container {
    max-width: 800px;
    margin: 2rem auto;
    padding: 2rem;
}

.container h1 {
    color: #00d4ff;
    margin-bottom: 2rem;
    font-size: 2rem;
}

.form-card {
    background: rgba(26, 26, 46, 0.6);
    border: 1px solid rgba(0, 212, 255, 0.3);
    border-radius: 8px;
    padding: 2rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    color: #00d4ff;
    font-weight: 500;
    margin-bottom: 0.5rem;
}

.form-input {
    width: 100%;
    padding: 0.75rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(0, 212, 255, 0.3);
    border-radius: 4px;
    color: #e0e0e0;
    font-size: 1rem;
    transition: all 0.3s ease;
}

.form-input:focus {
    outline: none;
    border-color: #00d4ff;
    background: rgba(255, 255, 255, 0.1);
    box-shadow: 0 0 0 3px rgba(0, 212, 255, 0.1);
}

.form-input::placeholder {
    color: rgba(224, 224, 224, 0.5);
}

.form-actions {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
}

.btn {
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 4px;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
    text-align: center;
    transition: all 0.3s ease;
}

.btn-primary {
    background: linear-gradient(135deg, #00d4ff, #0099cc);
    color: #0f0f1e;
}

.btn-primary:hover {
    background: linear-gradient(135deg, #00ffff, #00d4ff);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(0, 212, 255, 0.4);
}

.btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: #e0e0e0;
    border: 1px solid rgba(0, 212, 255, 0.3);
}

.btn-secondary:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: #00d4ff;
}
</style>